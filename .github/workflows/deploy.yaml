name: Cerita nya ini CI/CD Sistem Antrian

on:
  push:
    branches:
      - octane

jobs:
  deploy:
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v3
      - name: Pull and build Docker
        run: |
          cp C:\Users\SMAKA-SERVER\ppdb\sistem-antrian\docker\env\.env.prod C:\Users\SMAKA-SERVER\ppdb\actions-runner-sa\_work\sistem-antrian\sistem-antrian\docker\env\.env.prod
          ls
          git pull
          docker-compose up -d --build
